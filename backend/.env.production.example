# Production Environment Variables
# Copy these to your Cloud Provider's Environment Variables (e.g. Railway/Render)

# --- REQUIRED ---
# Your OpenAI API Key (or other LLM provider)
OPENAI_API_KEY=sk-proj-...

# A strong random string for JWT signing
# Generate with: openssl rand -hex 32
JWT_SECRET_KEY=change-this-to-a-secure-random-string

# --- OPTIONAL / DEFAULTS ---

# The Port the app runs on (Cloud providers usually set this automatically)
# PORT=8000

# Origins allowed to access the API (CORS)
# Set this to your frontend domain or extension ID if possible, or * for public
CORS_ORIGINS=["*"]

# Admin keys for the dashboard (comma-separated)
# SAFEBROWSE_API_KEYS=admin-key-1,admin-key-2

# Log Level
LOG_LEVEL=INFO
